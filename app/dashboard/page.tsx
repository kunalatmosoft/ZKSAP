import { Dashboard } from '@/components/dashboard';

export const metadata = {
  title: 'Dashboard - Crypto Auth',
  description: 'Your secure dashboard',
};

export default function DashboardPage() {
  return <Dashboard />;
}
